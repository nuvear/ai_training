# Decision log

Append-only. Agents record any interpretation made where docs were ambiguous.

| Date | Decision | Made by | Rationale / doc reference |
|---|---|---|---|
| 2026-07-07 | Hosting default set to Vercel + Neon pending owner confirmation | design session | tasks/M6.md |
| 2026-07-08 | Auth = custom email magic-link (token table + signed JWT session cookie via `jose`), not Auth.js. In dev the magic link is logged to the server console; Resend sends it when `RESEND_API_KEY` is set. | M0 build | tasks/M0.md §3 — full control, e2e-testable without a live mail server, one fewer heavy dependency |
| 2026-07-08 | RLS enforcement pattern: app connects to Postgres as a superuser role for simplicity, but every tenant query runs inside a transaction that does `SET LOCAL ROLE workshopos_app` (a NOLOGIN, non-superuser role) plus `set_config('app.current_*', …, true)`; RLS policies read those GUCs. `owner`/`staff` are global (see all orgs); `org_admin`/`participant` are org-scoped. | M0 build | CLAUDE.md invariant 8, DATA_MODEL §2 — proves org isolation without dual DATABASE_URLs; superuser bypasses RLS, but `SET LOCAL ROLE` to a non-superuser re-subjects the session to it |
| 2026-07-08 | Job queue = pg-boss (Postgres-backed), taking the "avoid Redis" option in CLAUDE.md. Uses `DIRECT_URL` (superuser) so it can create its `pgboss` schema. | M0 build | CLAUDE.md stack line, tasks/M0.md §8 |
| 2026-07-08 | Dev database = Docker Compose `pgvector/pgvector:pg16` on port 5433 (`pnpm db:up`), so a fresh clone reproduces the exact DB with pgvector + citext regardless of the host's Postgres. `pnpm db:migrate` brings it up first. | M0 build | tasks/M0.md exit criteria — reproducible "fresh clone" path |
| 2026-07-08 | i18n routing = next-intl with a `[locale]` path segment (`/en/…`, `/ja/…`); the locale switcher swaps the segment while preserving the rest of the path + search params. | M0 build | tasks/M0.md §4, DESIGN §4 |
| 2026-07-08 | Founder katakana **confirmed by owner**: ラジクマール・ラジャゴバラン (middle-dot separator, source name order). Resolves the ⚠ OPEN item in DESIGN §2.1. Use consistently in all JA founder-attribution surfaces. | owner | DESIGN §2.1 |
| 2026-07-08 | Localization engine (`content.localize`) uses the Anthropic API (`claude-opus-4-8`) when `ANTHROPIC_API_KEY` is set; otherwise a deterministic offline stub echoes the source text so the draft→review→publish pipeline works in dev/CI/e2e. Either way the target locale is flagged `ai_localized` until a human marks it reviewed. Input is treated strictly as data (injection defense, invariant 7). | M1 build | tasks/M1.md §7, COPILOT_TOOLS §2 |
| 2026-07-08 | USD stored as integer **cents**; JPY as whole yen (no minor units). Honors the money invariant; catalog formats USD `$310.00`, JPY `¥45,000`. | M1 build | DATA_MODEL conventions, DESIGN §7 |
| 2026-07-08 | Workshop publish gate: `title` and `summary` must have both locales; any other content field (`description`, `outcomes`) that is filled in one locale must be filled in the other. A `workshop.publish` that fails throws the typed `BilingualIncompleteError`, recorded on the ledger as `failed`. | M1 build | CLAUDE.md invariants 1 & 6 |
| 2026-07-08 | Tool tiers implemented per COPILOT_TOOLS §1–§2: `workshop.create/update`, `content.generate/localize`, `session.update_agenda`, `waitlist.promote` = auto; `workshop.publish/archive`, `cohort.create/clone`, `content.mark_reviewed` = approve; `cohort.cancel` = owner. UI invokes all tools through one endpoint `POST /api/tools/execute` — auto-tier runs immediately, approve/owner-tier lands in the approval queue. | M1 build | COPILOT_TOOLS §1–§2, §8 |
| 2026-07-08 | Payment processor changed from **Stripe to Airwallex** (owner decision, overriding the CLAUDE.md "decided" stack for the processor only). Airwallex Hosted Payment Page / Drop-in keeps card data off our servers, so invariant 4 holds (reworded processor-agnostic). Verified Airwallex supports Konbini (JPY, ¥1–¥300,000, async pay-at-store confirmed by webhook, 7-day expiry, full/partial refunds) and bank transfer for §4.2. Integration sits behind a thin PaymentProvider interface; webhook route `/api/webhooks/airwallex` (signature-verified, idempotent on `provider_ref`). Env: `AIRWALLEX_CLIENT_ID` / `AIRWALLEX_API_KEY` / `AIRWALLEX_WEBHOOK_SECRET` / `AIRWALLEX_ENV`. | owner + M2 build | tasks/M2.md, DATA_MODEL §4 |
| 2026-07-08 | **Promo `value` semantics**: whole percent for `percent`/`early_bird`/`seat_limited` (20 == 20%); minor units in the order currency for `fixed`/`referral`. Discount floored to an integer and clamped to `[0, subtotal]`. One code max per order. Redemption cap enforced transactionally via a conditional `updateMany (redeemed_count < max_redemptions)` in `claimPromo` (DATA_MODEL §8), not just app code. | M2 build | tasks/M2 test vector HAYAWARI20 → ¥39,600; PRODUCT_SPEC §4.3 |
| 2026-07-08 | **Dynamic tool tier**: added optional `dynamicTier?(input): Tier` to `ToolDefinition`. `ledger.effectiveTier(def, parsedInput) = def.dynamicTier?.(input) ?? def.tier` is the single authority; `proposeAction` stores it on the ai_action, `approveAndExecute` recomputes it from the STORED input, `proposeAndAutoExecute` and `/api/tools/execute` branch on it. `refund.execute` uses it: `approve` when amount ≤ ¥10,000 (JPY) / ≤ 10,000 USD-cents equiv, else `owner_only` (PRODUCT_SPEC §7). Tier stays server-authoritative — a caller cannot escalate or lower it. | M2 build | PRODUCT_SPEC §7, COPILOT_TOOLS §4 |
| 2026-07-08 | **Refund threshold across currencies**: PRODUCT_SPEC §7 states the boundary in ¥10,000. Applied literally for JPY; for USD refunds a fixed 10,000-cent ($100) equivalent is used for *tiering only* (not an FX rate for money). | M2 build | PRODUCT_SPEC §7 (refund threshold ⚠ resolved to spec default) |
| 2026-07-08 | **`seatpool.create_offer` pre-payment states**: creates a seat_pool `order` in `awaiting_transfer` + a not-yet-funded `SeatPool` + an `issued` `Invoice`. `InvoiceStatus` has no `awaiting_transfer` value, so the *order* (not the invoice) carries `awaiting_transfer`; the invoice's unpaid state is `issued`. `SeatPoolStatus` has no "pending" value (`active`/`exhausted`/`expired`/`cancelled`), so the unfunded pool is stored as `cancelled` and flipped to `active` by `invoice.mark_paid → fulfillOrder`. Sequential invoice numbers (`INV-000001`) allocated under `pg_advisory_xact_lock` so concurrent issuers cannot collide on the `number` unique column. | M2 build | tasks/M2, DATA_MODEL §4 (enum constraints) |
| 2026-07-08 | **Airwallex API shapes** implemented from the task's VERIFIED spec + the Airwallex API reference (docs are JS-rendered SPAs / auth-gated OpenAPI, not scriptable-fetchable, so shapes were confirmed against the task brief which the owner marked verified). Endpoints: `POST /api/v1/authentication/login` (headers `x-client-id`/`x-api-key` → bearer `token`, cached ~25m); `POST /api/v1/pa/payment_intents/create` (`request_id`,`amount` in MAJOR units,`currency`,`merchant_order_id`,`metadata` → `{id, client_secret}`); HPP on `checkout(-demo).airwallex.com` with `intent_id`+`client_secret`; `POST /api/v1/pa/refunds/create`. Webhook signature (verified, implemented exactly): `HMAC-SHA256(AIRWALLEX_WEBHOOK_SECRET, x-timestamp + rawBody)` hex, constant-time compared to `x-signature`, timestamp skew ≤ 5m, verified BEFORE JSON parse. Money: JPY minor==major; USD major = cents/100. Doc URLs referenced: https://www.airwallex.com/docs/api (Authentication, Payment Acceptance → Payment Intents, Refunds), https://www.airwallex.com/docs/payments__guides__hosted-payment-page, https://www.airwallex.com/docs/developer-tools__listen-for-webhook-events. | M2 build | tasks/M2 §1, invariant 3/4 |
| 2026-07-08 | **PDF invoice rendering DEFERRED** to M3: `invoice.issue`/`seatpool.create_offer` store a deterministic `pdfStorageKey` (`invoices/INV-000001.pdf`) as a stub; the actual bilingual 請求書 PDF render job lands with the M3 org portal. No PDF bytes are produced yet. | M2 build | tasks/M2 §3 (clean stub, deferred item noted) |
| 2026-07-08 | **B2C `bank_transfer` checkout radio** routes to the card-family hosted page: the `PaymentProvider.createHostedCheckout` `methods` set exposes only `card`/`konbini`, so the mock's "Invoice / bank transfer (orgs)" B2C radio settles via card until a dedicated transfer method exists. The genuine B2B 振込 path is the seat-pool → `seatpool.create_offer` → `invoice.issue` → `invoice.mark_paid` reconciliation flow, not B2C checkout. | M2 build | design mock screenCheckout, COPILOT_TOOLS §4 |
| 2026-07-08 | **Live-verified**: full Airwallex sandbox card payment (USD $248 = $310 −20% HAYAWARI20, no USD consumption tax) proven end-to-end — real hosted checkout → signed webhook via ngrok → signature verified → order `paid` + enrollment `active` + bilingual receipt, idempotent across the created/pending/authorized/succeeded event burst. e2e uses the mock provider (Airwallex keys forced blank) so CI needs no keys. | owner + M2 build | tasks/M2 exit criteria (card path) |
| 2026-07-08 | **DEFERRED (backlog) — Konbini voucher delivery**: for a JPY Konbini order Airwallex returns pay-at-store instructions (voucher reference/barcode + expiry, ~7-day window). The buyer-facing flow should capture those on the `/checkout/complete` page AND generate a shareable **QR/voucher PDF** the buyer can email/WhatsApp and present at the konbini (Lawson/FamilyMart/Ministop/Seicomart). Not built yet — owner flagged it during M2 testing; to be scheduled (candidate: alongside the M3 PDF-render work or a dedicated pass). Note Konbini is JPY-only, so it never appears on USD orders. | owner (M2 test feedback) | PRODUCT_SPEC §4.2, Airwallex Konbini docs |
| 2026-07-08 | **M3 tool tiers** (COPILOT_TOOLS §5–§6): `org.create` = approve (creates a billing entity; name validated bilingual via i18n-content). `org.assign_seats` = auto (upserts `participant` Users on the pool's org — email lowercased — and sends a locale-aware invite in 敬語 for JA; does NOT consume seats; only the pool's own org may be targeted). `participant.enroll` = **dynamic tier** `auto` for `self_paid`/`org_seat`, `approve` for `comp` (via `dynamicTier`, recomputed server-side from stored input). `report.org_progress` = auto. | M3 build | COPILOT_TOOLS §5–§6, PRODUCT_SPEC §7 |
| 2026-07-08 | **Seat consumption at enrollment**: `participant.enroll` with `source:'org_seat'` consumes one seat via a conditional `updateMany (seats_used < seats_total)` inside the tool's transaction — the enforcement point, backed by the DB CHECK `seats_used <= seats_total`. Exhaustion matches 0 rows → `InvalidStateError` (ledger `failed`), no over-allocation under concurrency. Re-running for an existing (user,cohort) enrollment is idempotent and consumes no seat. Pool must be `active`. | M3 build | tasks/M3 §2, DATA_MODEL §8 |
| 2026-07-08 | **Org-progress privacy boundary** (PRODUCT_SPEC §2, THE column-level gate): `getOrgProgress`/`readOrgProgress` return org KPIs (seatsUsed/seatsTotal from the active pool, completions, `avgRating` as a NUMBER = mean of `Feedback.ratings.overall`) + per-employee enrolled workshops/status/completions. It **never SELECTs `Feedback.textBody`** (free-text reflections) — the projection is `ratings` only. Attendance % and quiz scores are M4 tables, returned as explicit `null`/"pending" placeholders, never fabricated. | M3 build | PRODUCT_SPEC §2, feedback migration note |
| 2026-07-08 | **Org-progress runs elevated but self-scoped**: the enrollment RLS policy keys on the enrollee's own user id, so an `org_admin` context cannot read its employees' enrollments — yet an org admin is explicitly permitted to see aggregate completion for their org (§2). Like `fulfillment`, `readOrgProgress`/`generateOrgReport` therefore run under an OWNER-level `withOrgContext` but are SELF-SCOPED: every query filters by the `organizationId` argument (and employee ids derived from it), so no other org's rows can surface. Access is gated at the route by `requireOrgAccess` (org_admin of that org, or owner/staff); the column gate + explicit org scoping are the data guards. | M3 build | PRODUCT_SPEC §2, CLAUDE.md invariant 8, src/server/domain/fulfillment.ts precedent |
| 2026-07-08 | **Bilingual PDF rendering** (fulfilling the M2-deferred stub): `@react-pdf/renderer` with a bundled Japanese font `assets/fonts/NotoSansJP-Regular.ttf` (Noto Sans JP, TrueType/glyf variable font from google/fonts, `Font.register`ed once via `src/server/pdf/fonts.ts`). Rendered ON DEMAND (no object storage): `GET /api/invoices/[id]/pdf` (bilingual 請求書, `qualified_invoice_no` from env, blank-safe; money read from the Order, never recomputed; Latin numerals, ¥) and `GET /api/org/[id]/report.pdf` + `report.html` (bilingual monthly report, founder authored-by line). Verified output starts with `%PDF`, is non-trivial, and embeds a font subset (`FontFile`) so JA glyphs render. `report.org_progress`'s narrative and the report routes share one builder (`buildOrgReport`/`generateOrgReport`) — UI/tool/route never fork logic. | M3 build | tasks/M3 §3, DESIGN §2.1, CLAUDE.md invariant 3 |
| 2026-07-08 | **Org-admin tool allowlist on `/api/tools/execute`**: the copilot execute route is staff-only, but an `org_admin` legitimately drives `org.assign_seats` from the org portal. The route now allows `org_admin` for a small allowlist (`ORG_ADMIN_TOOLS = {org.assign_seats}`); RLS still confines the call to the caller's own org (the tool resolves the seat pool under the org_admin's tenant context, so a foreign pool id is invisible). All other tools stay staff-only. | M3 build | COPILOT_TOOLS §5, "UI and agents call the same internal API" |
| 2026-07-08 | **Monthly-report cron DEFERRED**: `report.org_progress` generates the bilingual report on demand (tool + `/report.pdf` + `/report.html`), which satisfies the M3 exit criterion. Wiring it to a monthly pg-boss schedule that emails each org on the 1st is a small follow-up (candidate: M5 AI-operations or a dedicated pass). e2e is forced fully offline (Airwallex/Anthropic/Resend keys blanked for the Playwright web server) so CI is deterministic and needs no keys. | M3 build | tasks/M3 §5 (generation done; scheduling follow-up) |
| 2026-07-08 | **M4 tool tiers + surfaces** (COPILOT_TOOLS §2/§5/§7): copilot-surface auto tools = `attendance.record`, `quiz.generate`, `certificate.issue`, `nudge.send`, `participant.search`; `quiz.publish` = approve (bilingual review gate). Concierge-surface (`surfaces:['concierge']`) §7 subset = `catalog.search`, `cohort.availability`, `enrollment.begin_checkout`, `promo.validate`, `faq.answer`, `handoff.to_human`. These six are registered ONLY for the concierge surface, so `assertSurfaceAllowed` structurally blocks the public chatbot from any copilot-only tool; the concierge runner routes every call through `assertSurfaceAllowed(name,'concierge')` plus a defense-in-depth §7 allow-list. | M4 build | COPILOT_TOOLS §2/§5/§7, CLAUDE.md invariant 2/7 |
| 2026-07-08 | **Completion → certificate → verify**: `evaluateCompletion(tx, enrollmentId)` (src/server/domain/completion.ts) computes attendance% = present/total cohort sessions and quiz = best passing attempt; completed iff attendance% ≥ `workshop.completionRule.attendance_pct` (default 80) AND a passing attempt exists (quiz `passPct` defaults from `completionRule.quiz_pass_pct`, default 70). Per-workshop overrides honored; the stored JSON is untrusted config, each field validated to a finite [0,100] number else default; 0 sessions ⇒ attendance gate unmet (never 0/0=complete). On first satisfaction: enrollment `completed_at`+status `completed`, `ProgressEvent(completed)`, and an idempotent certificate (unique per enrollment via `certificate.enrollment_id`; random URL-safe base64url `verifyCode`). Participant-driven paths (self check-in, quiz attempt) run completion under an OWNER-level `withOrgContext` that is SELF-SCOPED to the one enrollmentId (fulfillment.ts precedent) because it crosses the enrollment/cohort/session boundary. Public `GET /api/verify/[code]` reads by verifyCode via base prisma and returns ONLY {valid, participantName(display-name), workshopTitle, issuedAt} — never email/org/scores. | M4 build | PRODUCT_SPEC §5, COPILOT_TOOLS §5, CLAUDE.md invariant 8 |
| 2026-07-08 | **Quiz generation stub + injection defense**: `quiz.generate` reads the workshop's actual `content/ideage/<slug>/workshop{,.ja}.md` body (frontmatter-stripped) plus title/summary/skills and generates a bilingual MCQ quiz via Anthropic (claude-opus-4-8) when `ANTHROPIC_API_KEY` is set, else a DETERMINISTIC bilingual stub whose questions/options/explanations are derived from the workshop title/summary/skills (correct key rotates so "always A" fails). Lands `Quiz(ai_draft)`+`QuizQuestion`s; `passPct` seeded from the workshop's `quiz_pass_pct`. AI path wraps material in `<material>` tags and instructs treat-as-data-only; any AI/parse failure falls back to the stub. `quiz.publish` bilingual-gates title + every prompt/option/explanation before ai_draft→published. Attempts scored server-side only (`score=correct/total×100`, `passed=score≥passPct`). | M4 build | COPILOT_TOOLS §2, PRODUCT_SPEC §5, CLAUDE.md invariant 7 |
| 2026-07-08 | **Public concierge** (`POST /api/concierge`, no auth): runs an Anthropic tool-use loop when keyed, else a deterministic keyword→tool intent router; either way it can ONLY call §7 tools (surface gate on every call) and ALWAYS self-identifies as AI. Anonymous visitors are allowed; a session cookie, when present, lets `enrollment.begin_checkout` hand off to the shared `beginCheckout` (concierge never touches payment data); an anonymous checkout request degrades to `handoff.to_human`. `handoff.to_human` opens a ticket as a concierge `ai_action` row (the ledger is the ticket store — no schema change). `faq.answer` retrieves over PUBLISHED workshops only and prefixes an AI self-identification. | M4 build | COPILOT_TOOLS §7, PRODUCT_SPEC §7 |
| 2026-07-08 | **M4 BASIC items noted**: `participant.search` is a BASIC name/email ILIKE search respecting RLS scope; pgvector semantic ranking DEFERRED. `nudge.send` uses fixed bilingual substitution-safe templates (only the trusted workshop title is interpolated), respects `user.notificationPrefs` mute flags (`muteAll` / `mutedKinds[]` / `nudges.<kind>=false`), and writes `ProgressEvent(nudge_sent)`. Certificate PDF rendered on demand (`GET /api/certificates/[enrollmentId]/pdf?locale=`, guard: enrollment owner or staff) with the bundled Noto Sans JP font, carrying the founder signature line "Rajkumar Rajagobalan, Facilitator" / "ラジクマール・ラジャゴバラン（ファシリテーター）". No UI built (frontend agent owns dashboard/verify page/concierge chat) — all tools+routes are API-drivable. | M4 build | tasks/M4.md, DESIGN §2.1 |
| 2026-07-08 | **M5 copilot orchestrator** (`src/server/ai/orchestrator.ts`, PRODUCT_SPEC §7 / COPILOT_TOOLS §1): `planCommand(session, command, locale)` writes a PARENT ai_action (`surface:'copilot'`, `toolName:'copilot.plan'`, `tier:'auto'`, `input:{command,locale}`, `proposed`) then one CHILD per step (`parentId`=parent, tier read from the registry via `effectiveTier` — the model can only PROPOSE tools, never set tiers or execute). Real path = Anthropic tool-use exposing ONE tool `submit_plan({steps:[{tool,input,rationale}]})`; each returned `tool` is validated to exist AND be copilot-surfaced, tier taken from the registry, non-allowed tools dropped. Stub path (no key / CI) = deterministic EN+JA parser that recognizes the September-launch pattern (sessions/price/cap/promo%/until, workshop resolved by slug `design-thinking`) and emits the canonical 5 steps; unrecognized commands yield an empty plan (no crash), never a fabricated action. Command NL is treated strictly as DATA. | M5 build | tasks/M5, COPILOT_TOOLS §1 |
| 2026-07-08 | **Plan `$ref` threading + dependency-aware execution**: a child input value may be `{ $ref:'step:N.field' }`, resolved at execution time from step N's executed output (step 4 `cohortId ← step:1.cohortId`, step 5 `campaignId ← step:4.campaignId`). `runPlan(session, parentId)` is NOT strictly sequential: it runs every RUNNABLE `auto` step (all $ref deps already executed) to a fixpoint, leaves `approve`/`owner` steps `proposed` (approval queue) and dep-blocked steps `proposed` (pending); when all children are executed the parent flips to `executed`. Child seq is made deterministic by setting each child's `createdAt = parent.createdAt + seq ms` (strictly increasing) so `orderBy createdAt asc, id asc` is a stable seq for $ref resolution — no reliance on timestamp tie-breaking. | M5 build | tasks/M5 execution model |
| 2026-07-08 | **Approval resumes the plan (ledger extension)**: `approveAndExecute` was extended minimally — after a plan CHILD (has `parentId`) is approved+executed it dynamic-imports `orchestrator.resumePlan(parentId)` to unblock now-runnable auto steps; and `runAction` resolves a child's `$ref` inputs via `orchestrator.resolveChildInput(child)` BEFORE validation so an approved step sees concrete values from earlier steps. Dynamic import breaks the ledger↔orchestrator import cycle. All existing ledger/tier tests stay green. So the launch plan: initial `runPlan` executes step 2 (landing, no deps) and queues steps 1,3 (approve) + steps 4,5 (pending); approving step 1 unblocks+runs step 4 and queues step 5; approving 3 and 5 completes the plan (parent `executed`). | M5 build | tasks/M5 resume-on-approval |
| 2026-07-08 | **M5 narrative-analytics semantic layer (metric safelist)** — THE no-raw-SQL guard (COPILOT_TOOLS §6, M5 exit criterion 3). `src/server/ai/analytics/semantic-layer.ts` holds a fixed registry of six named METRICS, each a hand-written parameterized Prisma query returning `{ value, unit, evidence: Row[] }`: `sales_total` (Σ paid JPY `Order.total`), `enrollments_count`, `refund_total` (Σ JPY refunds), `completion_rate` (completed/total enrollments), `no_show_rate` (absent/total attendance), `avg_rating` (mean `Feedback.ratings.overall`, numeric only — never `textBody`). Timeframe is a fixed enum `week`\|`month`\|`quarter`\|`all` → `windowFor()` resolves a `[from,to]` Prisma DateTime filter. Money metrics never sum across currencies (JPY-only headline totals per invariant 3). Money read from stored `Order.total`, never recomputed. | M5 build | COPILOT_TOOLS §6, CLAUDE.md invariant 3 |
| 2026-07-08 | **Model output → metric selection can NEVER become SQL**: `analytics.query` calls `selectMetric(question, timeframe?)`, which with `ANTHROPIC_API_KEY` set asks the model to call ONE tool `select_metric({ metric: enum(METRIC_NAMES), timeframe: enum })` — structured output constrained to the safelist enum. The returned name is re-validated with `isMetricName()`; anything not in `METRIC_NAMES` is IGNORED and falls back to deterministic keyword selection (offline stub path). `getMetric()` is a second guard that throws unless the name is safelisted. The question text is treated strictly as DATA (keyword matching only) — there is no code path anywhere that turns a question, a metric name, or any model output into raw SQL, `$queryRaw`, or a dynamic filter beyond the enumerated `{from,to}` window (+ an optional self-validated UUID `workshopId`). `analytics.explain_change({ metric: enum, period: enum })` compares the metric's current vs prior equal-length window (`priorWindowFor`) and returns a deterministic bilingual narrative + both periods' evidence rows; same safelist constraint (zod enum + `isMetricName` + `getMetric`). Proven by `tests/analytics-safelist.test.ts` (SQL-in-question leaves the `order` table intact, row count unchanged; out-of-safelist names rejected). | M5 build | COPILOT_TOOLS §6 "never raw SQL from the model", CLAUDE.md invariant 7 |
| 2026-07-08 | **M5 predictive & feedback tools** (COPILOT_TOOLS §6, all `auto`, copilot surface): `feedback.cluster` groups a cohort's Feedback by rating band (promoters ≥5 / passives 3–4 / detractors <3 / unrated), derives sentiment in [-1,1] from the numeric `ratings.overall` only (never sends `textBody` to any model — injection defense, invariant 7), and writes `aiTheme`/`aiSentiment` back to each row; `provider:'stub'` (Anthropic theme-labeling is a future enhancement that would not change the contract). `predict.no_shows` is a transparent heuristic per active enrollment: base 0.1 + prior absences + missing `prework_done` event + no attendance-yet → score∈[0,1] bucketed low/medium/high (feeds reminder escalation), highest-risk first, with human-readable `reasons`. `predict.demand` blends recent (30d) paid orders + attribution visits + open-cohort fill rate into a bounded score∈[0,1] labeled cold/warm/hot and emits `suggestedObjective` (`fill_seats`/`early_bird`/null) to feed `promo.suggest`. No ML — simple thresholds, not tuned. Semantic-search over embeddings stays DEFERRED (no embeddings provider configured); `participant.search` keeps its M4 ILIKE implementation untouched. | M5 build | COPILOT_TOOLS §6, PRODUCT_SPEC §6, tasks/M5 "do not over-engineer" |
| 2026-07-08 | **M5 marketing tool tiers** (COPILOT_TOOLS §3): `landing.generate_variants`=auto (N `LandingPage` `ai_draft` with campaign-register blocks per DESIGN §1.1 — hero/OUTLINE/VOICES/DETAILS/FLOW + repeated CTA, bilingual, deterministic stub from workshop title/summary/outcomes, variants A/B differ in hero+CTA copy, trafficWeight split 1/n), `landing.publish`=approve (→ live), `landing.reallocate_traffic`=auto (post-significance; updates `trafficWeight` and logs per-variant weight evidence in the ai_action output), `campaign.draft_sequence`=auto (creates `Campaign(draft)` with a 3-email bilingual announce sequence [{subject,body,offsetDays}] → `{campaignId}`), `campaign.approve_and_schedule`=approve (one approval → status `approved` + `scheduledAt`, defaults to now), `social.draft_posts`=auto (returns bilingual drafts + `autoPosted:false`; NEVER calls any social API, spec §6). Routes: `POST /api/copilot/plan` (planCommand) and `POST /api/copilot/plan/[parentId]/run` (runPlan), both `requireRole(...STAFF_ROLES)`; individual-step approval reuses `/api/approvals/[id]/approve`. Seed adds a published bilingual `design-thinking` workshop (fixed id 00000000-0000-0000-0000-00000000d711) + 3 skills so the launch command resolves a real workshop. | M5 build | COPILOT_TOOLS §3, DESIGN §1.1, PRODUCT_SPEC §6 |
| 2026-07-07 | **Founder katakana OWNER-CONFIRMED**: the spelling ラジクマール・ラジャゴバラン (family-name order preserved, middle-dot separator) was explicitly confirmed by the owner in-session (DESIGN §2.1 ⚠ resolved). Applied consistently across `messages/ja.json` (Catalog.facilitator, Home.byline), the certificate/invoice/report PDFs, `org-report.ts`, and the marketing/orgs tools. This is the one identity item the system may not decide on its own — now settled by the owner, not inferred. | M6 build | DESIGN §2.1, M6 §3 i18n katakana confirmation |
| 2026-07-09 | **M6 dependency-audit finding — next-intl advisories accepted, not exploitable**: `pnpm audit` reports 3 moderate advisories against `next-intl` 3.26.x (GHSA-8f24-v5vv-gm5j, GHSA-4c35-wcg5-mm9h — prototype pollution via `experimental.messages.precompile` on attacker-controlled catalog KEYS). Assessment: we do NOT enable `experimental.messages.precompile` anywhere (grep-clean), and translation catalogs are first-party static JSON (not attacker-controlled), so the vulnerable path is unreachable in our config. Fix requires next-intl 4.9.2+ (a 3.x→4.x MAJOR upgrade with breaking routing/config changes). DECISION: accept for launch (documented residual risk), schedule the 4.x upgrade as the first post-launch maintenance item — tracked in docs/RUNBOOK.md. No hardcoded secrets in tracked files (scan clean); `.env` gitignored and untracked. | M6 build | M6 §1 dependency audit + secrets scan |
| 2026-07-09 | **M6 §1 authz matrix + rate limiting**: THE go-live authz exit criterion is `tests/authz-matrix.test.ts` — it enumerates EVERY registered tool × EVERY role (owner/staff/org_admin/participant), encodes the expected invocation policy as data, and asserts the REAL guards (route role gate `allowedInvokeRoles` + `assertSurfaceAllowed('copilot')` + `canApprove`) match with ZERO unexpected allows (200 tool×role pairs asserted over 50 tools; a `not-in-expected-set` allow FAILS the suite). Invocation policy: copilot-surfaced tools allow STAFF_ROLES, PLUS org_admin only for `ORG_ADMIN_TOOLS={org.assign_seats}`; participant never; concierge-only tools invocable by NO role via execute. Approval: owner clears approve+owner_only, staff approve-only, org_admin/participant neither; dynamic-tier branches (refund.execute ≤/>¥10k, participant.enroll comp vs self/org) asserted both ways. Moved `ORG_ADMIN_TOOLS` to `src/server/ai/authz.ts` (Next route files may only export handlers) so route + test share one source. Rate limiting: in-memory sliding-window `src/server/rate-limit.ts` keyed by client IP (`x-forwarded-for` first hop) × route bucket, applied to the 4 public endpoints — magic-link **5/15min**, concierge **20/min**, verify **60/min**, checkin **30/min**; on exceed → **429 + `Retry-After`** header + friendly JSON. Blocked requests are NOT recorded (cannot self-extend the window). In-memory suits a single instance; a pg/Redis shared store is the multi-instance upgrade behind the same `checkRateLimit` interface. | M6 build | M6 §1, CLAUDE.md invariant 2/7 |
| 2026-07-09 | **M6 §4 observability metric definitions**: `getAiLayerMetrics(tx?, scope?)` (`src/server/observability/metrics.ts`) computes from the `ai_action` ledger — totalActions, byTier, byStatus, and the trust metrics: **approvalRate** = (approved∪executed) / human-decided, over actions that REQUIRED approval (tier approve|owner_only) and reached a terminal decision (approved|executed|rejected); **overrideRate** = rejected / human-decided over the same set (== 1−approvalRate); **autoShare** = auto-tier executed / all executed; **latency** = avg + p50 of (updatedAt−createdAt) ms over executed rows. Optional `scope.where` lets tests namespace to their own seeded rows on the shared append-only ledger (production passes nothing = whole table). Exposed staff-only at `GET /api/admin/metrics` (requireRole owner/staff). Structured JSON logger `src/server/observability/logger.ts` (`log.info/warn/error`, one JSON object per line) + `captureError(err,ctx)` — logs structured error JSON now, Sentry-ready seam guarded by `SENTRY_DSN` (no-op until SDK+DSN provisioned); wired into `http.ts` `toErrorResponse` 500 branch. `SENTRY_DSN` added (commented) to `.env.example`. | M6 build | M6 §4, CLAUDE.md invariant 2 |
| 2026-07-09 | **Rate-limit test escape hatch**: `enforceRateLimit` returns null (no limiting) when `process.env.RATE_LIMIT_DISABLED === '1'`. Set ONLY in the e2e webServer env (playwright.config.ts) because the suite signs in many times from one localhost IP and would legitimately trip magic-link 5/15min. Production never sets it. The limiter ALGORITHM stays fully covered — `tests/rate-limit.test.ts` drives `checkRateLimit` directly (injected clock), unaffected by this route-level gate; the vitest suite runs with limiting ON. | M6 build | M6 §1 rate limiting vs deterministic e2e |
